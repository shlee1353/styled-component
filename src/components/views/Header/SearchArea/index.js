import React from "react";
import styledComponents from "./SearchArea-style";

const { 
    CustomSearchArea,
} = styledComponents;

const SearchArea = () => {
  return (
    <CustomSearchArea>
        search
    </CustomSearchArea>
  );
};

export default SearchArea;
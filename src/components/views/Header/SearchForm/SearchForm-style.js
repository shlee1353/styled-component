import styled, { css } from "styled-components";

const CustomSearchForm = styled.form`
    position: relative;
    height: 100%;
`;

const styledComponents = { 
    CustomSearchForm,
};

export default styledComponents;